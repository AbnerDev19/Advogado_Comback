/**
 * Configuração central da API.
 *
 * IMPORTANTE: troque o valor abaixo pela URL real do backend depois do deploy
 * (ex: Render, Railway, etc). Antes esta URL estava duplicada e "quebrada"
 * (https://SUA-API-NO-RENDER.onrender.com) em 4 arquivos diferentes
 * (script.js, login-script.js, dashboard-script.js), o que fazia todas as
 * chamadas à API falharem silenciosamente. Agora há um único lugar para editar.
 */
window.API_BASE_URL = 'https://SUA-API-NO-RENDER.onrender.com';
